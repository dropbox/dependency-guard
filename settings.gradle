include ":sample:app"
include ":sample:module1"
include ":sample:module2"

rootProject.name = "dependency-guard-root"

includeBuild('dependency-guard-gradle-plugin') {
  dependencySubstitution {
    substitute module('com.dropbox.dependency-guard:dependency-guard') with project(':')
  }
}
